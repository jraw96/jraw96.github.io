//template.js

function template(colour, initialSpotInCanvas) {
    this.colour = colour;
    this.initialSpotInCanvas = initialSpotInCanvas;
}

template.prototype.draw = function(mouse){
};

template.prototype.scan = function(mousePosition){
}

template.prototype.move = function(moveX,moveY){
}